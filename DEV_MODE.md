# 🔓 开发者模式说明

## ✅ 已启用开发者模式

在开发环境（`npm run dev`）下，已自动启用开发者模式，**无需任何手动操作**。

---

## 🎯 开发者模式特权

### 1. 无频率限制
- ✅ 可以无限次使用诊断功能
- ✅ 不受 24 小时一次的限制
- ✅ 方便开发和测试

### 2. 自动启用
- ✅ 开发环境下自动检测并启用
- ✅ 无需点击 Logo 7 次
- ✅ 无需手动设置

---

## 🔍 实现原理

### 代码逻辑

```typescript
const checkIfDevDevice = () => {
  // 如果是 Vite 开发环境，自动启用
  if (import.meta.env.DEV) {
    localStorage.setItem('DEV_ACCESS_KEY', 'true');
    return true;
  }
  return localStorage.getItem('DEV_ACCESS_KEY') === 'true';
};

const handleStart = () => {
  const isDev = checkIfDevDevice();
  const lastDiagnosis = localStorage.getItem('lastDiagnosisTime');
  
  // 开发者模式下跳过频率检查
  if (lastDiagnosis && !isDev) {
    const timeSince = Date.now() - parseInt(lastDiagnosis);
    if (timeSince < 24 * 60 * 60 * 1000) {
      setStep(AppStep.RATE_LIMITED);
      return;
    }
  }
  setStep(AppStep.INPUT);
};
```

### 环境检测

- **开发环境**：`import.meta.env.DEV === true`
  - 运行 `npm run dev` 时
  - 自动启用开发者模式
  - 无频率限制

- **生产环境**：`import.meta.env.DEV === false`
  - 运行 `npm run build` 后
  - 开发者模式不会自动启用
  - 保持 24 小时限制

---

## 🧪 验证开发者模式

### 方法 1：查看浏览器控制台

1. 打开浏览器开发者工具（F12）
2. 切换到 Console 标签
3. 输入并执行：
   ```javascript
   localStorage.getItem('DEV_ACCESS_KEY')
   ```
4. 应该返回：`"true"`

### 方法 2：测试频率限制

1. 完成一次诊断
2. 立即点击"开启今日问诊"
3. 应该能够直接进入输入页面
4. 不会显示"今天已经号过脉啦～"的提示

### 方法 3：查看 localStorage

1. 打开浏览器开发者工具（F12）
2. 切换到 Application 标签
3. 左侧选择 Local Storage → http://localhost:3000
4. 查看 `DEV_ACCESS_KEY` 的值
5. 应该是 `true`

---

## 🔄 环境切换

### 开发环境 → 生产环境

当你构建生产版本时：

```bash
npm run build
```

- ✅ 开发者模式自动禁用
- ✅ 恢复 24 小时限制
- ✅ 保护生产环境

### 生产环境手动启用（仅用于测试）

如果需要在生产环境临时启用开发者模式：

1. 打开应用
2. 连续点击 Logo 7 次
3. 控制台会显示：`"Internal: Developer mode enabled. Rate limit bypassed."`
4. 开发者模式启用

---

## 📊 状态对比

| 功能 | 普通用户 | 开发者模式 |
|------|---------|-----------|
| 诊断频率 | 24 小时一次 | 无限制 ✅ |
| 启用方式 | 无法启用 | 自动启用 ✅ |
| 环境 | 生产环境 | 开发环境 |
| localStorage | 无标记 | `DEV_ACCESS_KEY: true` |

---

## 🎨 用户体验

### 开发环境（你当前的环境）

```
用户点击"开启今日问诊"
  ↓
检测到开发者模式
  ↓
跳过频率检查 ✅
  ↓
直接进入输入页面
```

### 生产环境

```
用户点击"开启今日问诊"
  ↓
检查上次诊断时间
  ↓
如果 < 24 小时
  ↓
显示"今天已经号过脉啦～"
```

---

## 🔒 安全性

### 开发环境
- ✅ 自动启用，方便开发
- ✅ 不影响生产环境
- ✅ 仅在本地有效

### 生产环境
- ✅ 默认禁用开发者模式
- ✅ 保持频率限制
- ✅ 保护服务器资源

---

## 🐛 故障排查

### 问题：仍然显示频率限制

**解决方案**：

1. 确认正在运行开发服务器：
   ```bash
   npm run dev
   ```

2. 清除 localStorage：
   ```javascript
   // 在浏览器控制台执行
   localStorage.clear();
   ```

3. 刷新页面（Cmd+R 或 Ctrl+R）

4. 再次尝试诊断

### 问题：开发者模式未启用

**解决方案**：

1. 检查是否使用 `npm run dev` 启动
2. 打开控制台，手动设置：
   ```javascript
   localStorage.setItem('DEV_ACCESS_KEY', 'true');
   ```
3. 刷新页面

---

## 📝 总结

✅ **开发环境下，你现在可以**：
- 无限次使用诊断功能
- 无需等待 24 小时
- 快速测试和开发

✅ **生产环境下，自动恢复**：
- 24 小时频率限制
- 保护服务器资源
- 正常用户体验

---

**现在你可以随意测试诊断功能了！** 🎉
