/**
 * Mock 多视角诊断数据
 * 用于开发测试，当 AI 返回的数据中没有 multiViewDiagnosis 时使用
 */

import { MultiViewDiagnosis } from '../types';

export const mockMultiViewDiagnosis: MultiViewDiagnosis = {
  base_summary: "内容在积累，但标签还没收敛，算法还在观察你的方向",
  core_score: 6,
  diagnosis_modules: {
    content_view: {
      id: "content_view",
      title: "内容视角",
      content: "你的内容情绪唤起能力不错，能让人有共鸣。现在更需要的是在表达上更聚焦一些，让每条内容都在强化同一个标签。可以试试每条内容只讲一个点，让用户更容易记住。"
    },
    account_positioning_view: {
      id: "account_positioning_view",
      title: "账号定位视角",
      content: "简介和主页内容跨了几个方向，算法还在观察你想做什么。可以先从统一简介开始，明确「你是谁+做什么+给什么价值」，然后把主页前10条内容调整成同一个方向。"
    },
    user_psychology_view: {
      id: "user_psychology_view",
      title: "用户心理视角",
      content: "用户看到你的主页时，可能会有点困惑「这个账号到底是做什么的」。这不是内容不好，而是缺少一个清晰的预期。可以让用户在3秒内知道「关注你能得到什么」。"
    },
    growth_bottleneck_view: {
      id: "growth_bottleneck_view",
      title: "增长瓶颈视角",
      content: "目前的瓶颈不是流量，而是标签收敛。算法需要看到你连续输出同一方向的内容，才会把你推荐给精准人群。可以先坚持一个方向2-3周，看看数据变化。"
    },
    optimization_actions_view: {
      id: "optimization_actions_view",
      title: "优化行动视角",
      content: "今天可以做的：调整简介，明确身份标签。本周可以完成的：把主页前10条内容统一成同一个方向。容易被忽略的点：简介里尽量不要写「热爱生活」这类泛词，算法不太容易识别。"
    },
    case_reference_view: {
      id: "case_reference_view",
      title: "案例参考视角",
      content: "同类型账号通常会在简介中明确「身份+领域+价值」，比如「3年运营 | 教你用数据做决策」。然后前10条内容只讲一个方向，让算法快速打标签。你也可以参考这个思路。"
    }
  }
};

/**
 * 为诊断结果添加 Mock 多视角数据
 */
export const addMockMultiViewIfNeeded = (result: any): any => {
  // 如果已经有多视角数据，直接返回
  if (result.multiViewDiagnosis) {
    return result;
  }
  
  // 如果是开发环境，添加 Mock 数据
  if (import.meta.env.DEV) {
    console.log('🔧 开发模式：添加 Mock 多视角数据');
    return {
      ...result,
      multiViewDiagnosis: mockMultiViewDiagnosis
    };
  }
  
  return result;
};
